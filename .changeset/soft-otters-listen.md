---
'dle.dev': patch
---

Grant GitHub deployment write permission to the Pages workflows so Cloudflare's Pages action can create deployment records without failing with `Resource not accessible by integration`.
