---
'dle.dev': patch
---

Rename the GitHub deployment environments to Production, Staging, and Previews while keeping staging and branch previews on the `dle-dev-staging` Cloudflare Pages project without duplicate Cloudflare deployment records in the GitHub UI.
