---
'dle.dev': patch
---

Update the agent instructions to load skills dynamically, keeping Svelte guidance as the default baseline and loading design or GSAP-related skills only when the task requires them.
